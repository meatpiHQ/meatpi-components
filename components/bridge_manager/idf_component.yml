## Managed component dependencies for bridge_manager.
dependencies:
  # IDF v6 removed the bundled `json` component; cJSON comes from the
  # registry. Private: the public header exposes no cJSON types.
  espressif/cjson: "^1.7.18"
